- Create a project folder
- Create venv environment
- open terminal
- pip install django
- django-admin startproject smsmgtsys .
- python manage.py startapp userapp
- pip install pillow
- create folder - templates, static , media
- setting.py
	- installed_app

	INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'userapp',
]

	- templates
	'DIRS': ['templates'],

	- static
		STATIC_URL = 'static/'
		STATICFILES_DIRS = ['static']

	- media
		MEDIA_URL = '/media/'
		MEDIA_ROOT = BASE_DIR / 'media'